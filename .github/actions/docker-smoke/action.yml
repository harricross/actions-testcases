name: docker-smoke
description: Local Dockerfile action used by docker-action-test.yml. Linux-only.
inputs:
  who:
    description: Name to greet
    required: true
    default: world
outputs:
  greeting:
    description: The full greeting
runs:
  using: docker
  image: Dockerfile
  args:
    - ${{ inputs.who }}
